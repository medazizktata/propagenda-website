# Dependencies
node_modules/
.pnpm-store/

# Next.js
.next/
# Out-of-tree builds via NEXT_DIST_DIR (.next-prod, .next-cf-check, …)
.next-*
out/
.turbo/

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Env
.env
.env.local
.env*.local

# Research / reference (not part of deployed site)
reference/
docs/

# Backlog.md task tracking (local only, not part of the deployed site)
backlog/
prompts/
scripts/.venv-ig/
scripts/node_modules/
scripts/crawl-*.mjs
scripts/capture-*.mjs
claude/
AGENTS.md
Website-Replication-Strategy-chatgpt-chat.md
tools/pdf-extractor/
tools/**/.venv/

# Agent / IDE
.cursor/

# Python (pdf-extractor venv)
.venv/
tools/pdf-extractor/.venv/
__pycache__/
*.pyc
*.egg-info/

# OS / editors
.DS_Store
.idea/
.vscode/

# Debug
npm-debug.log*
pnpm-debug.log*
.ref/

# Playwright
test-results/
playwright-report/
blob-report/

# Local Playwright debug captures
.playwright-hero-*.png
.playwright-mcp/

# Perf reports (local Lighthouse HTML)
reports/*.html
reports/*.json
reports/lhci/
*.report.html
*.report.json
.vercel
.env*
.dev.vars
.open-next
.wrangler

# Raw video source dump (personal footage archive / drive exports staged for editing,
# not deployed site assets — the real served videos live under public/videos/).
/videos/

# Impeccable design-skill local scratch: review screenshots and the hook's own cache are
# throwaway per-session artifacts, not durable project config.
.impeccable/review/
.impeccable/hook.cache.json

# watermark pipeline build output (tools/watermark/process-inbox.cjs)
/.watermark-out/

# committed on purpose: public build flags only (see the file header)
!.env.production
