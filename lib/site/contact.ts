/** Public studio inbox — Cloudflare Email Routing alias. */
export const PUBLIC_CONTACT_EMAIL = 'contact@thepropagenda.com';

/** Resend delivery inbox — account email until Resend allows arbitrary recipients. */
export const CONTACT_DELIVERY_INBOX = 'propagendamarketing@gmail.com';

/** Cloudflare Turnstile site key (public; widget hostname allowlist in CF dashboard). */
export const TURNSTILE_SITE_KEY_PUBLIC =
  '0x4AAAAAAEXndoVcXl1or5zJ';
