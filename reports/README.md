# Performance reports (local only)

Lighthouse HTML reports from `pnpm perf:lighthouse` land here. Gitignored — do not commit.

```bash
pnpm dev          # :4000
pnpm perf:lighthouse
open reports/home.report.html
```

Also used by manual Chrome Lighthouse exports (`*.report.html`).
